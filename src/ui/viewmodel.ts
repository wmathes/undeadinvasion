/**
 * Knockout viewmodel factory.
 *
 * Phase 1 placeholder. Phase 2 will move the ko.observable / ko.computed
 * declarations currently scattered through app.ts (Core.Score,
 * UndeadInvasion.Game ko bindings) into this module so the UI binding
 * layer is isolated from game simulation code.
 */

export {};
